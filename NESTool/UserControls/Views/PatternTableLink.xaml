<UserControl x:Class="NESTool.UserControls.Views.PatternTableLink"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NESTool.UserControls"
             mc:Ignorable="d" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignWidth="100"
             d:DesignHeight="19">
    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
        <Hyperlink Command="{Binding SelectTileSetCommand}" CommandParameter="{Binding TileSetId}">
            <TextBlock x:Name="hlText" Text="{Binding Caption}" 
                       Style="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Hyperlink}, Path=Style}" />
        </Hyperlink>
    </TextBlock>
</UserControl>
